# Devir notu — 2026-09-15 03:08

Saat dilimi: Europe/Istanbul. Kapsam: yeni başlangıç/devir belgelerinin okunması, yerel çalışma ağaçlarının kimliklerinin doğrulanması ve GitHub ile commit/dosya farklarının çıkarılması.

## Repo ve commit sınırları

- Repo: [openai-altyazi-cevirisi](https://github.com/dorukakindev/openai-altyazi-cevirisi).
- Dal: `master`.
- Başlangıç ve bitiş kod commit'i: `1f690e7d909c6b27a292c610d78893cefdd2d965`.
- Bu çalışmada uygulama kodu değişmedi. Bu not ve `DEVIR-NOTU.md` ayrı belge commit'ine alınacaktır.

## Okunan belgeler ve çalışma ağacı kimlikleri

- `AGENTS.md`, `docs/YENI-KURULUM-VE-CALISMA.md`, `DEVIR-NOTU.md`, güncel `2026-09-14-0719.md` ve son teknik `2026-09-14-0253.md` okundu. Kurulum talimatının oluşturulduğu `2026-09-14-0711.md` de incelendi.
- Ana checkout `master` dalında `1f690e7d909c6b27a292c610d78893cefdd2d965` commit'indedir.
- Eski Codex checkout'u detached `85553a607f9d7ad95b1b611b14818ee884ef0244` commit'indedir. Bu commit güncel master'ın doğrudan atasıdır ve 271 commit geridedir.
- Ana checkout ve eski Codex checkout'unda önceden var olan izlenmeyen dosyalar görüldü. Bunlar değiştirilmedi, stage edilmedi veya temizlenmedi.
- Git kayıtlarında başka eski Claude/Codex worktree'leri de bulunmaktadır; bu çalışma hiçbirinde dal değiştirmedi veya birleştirme yapmadı.

## Fetch ve fark sonuçları

`git fetch --prune origin` başarılı oldu. Fetch sonrasında:

- Yerel `master` ve `origin/master` aynı commit'tedir: `1f690e7d909c6b27a292c610d78893cefdd2d965`.
- `git rev-list --left-right --count HEAD...origin/master` sonucu `0 0` oldu.
- Yerel veya uzak tarafta tek yönlü commit ve dosya farkı yoktur; pull/merge gerekmemektedir.
- Eski Codex checkout'u ile güncel master karşılaştırması `0 271` verdi. Dosya özeti 285 dosyada 86.638 ekleme ve 2.039 silmedir. Bu tarihsel fark uygulama geliştirmelerinin yanında testler, raporlar ve altyazı/arşiv içerikleri de içerdiğinden tek bir özellik paketi olarak yorumlanmamalıdır.

Son ortak Native Reader commit'i `bc2dc053f154d57ef431f2bff1508b9aa5ffe520` sonrasındaki daha yakın fark ayrıca incelendi:

- 7 commit; 23 dosya; 1.865 ekleme ve 86 silme.
- Asıl uygulama commit'i `0dc3347ac668987124fcf3a7f109e4bd7ebf814c`: deneme çevirisi, geçiş inceleme/geri alma, onaylı tercihler, ayrı pilot süreç, dosya kuyruğu ekleme/silme/temizleme ve Windows yol tekilleştirme düzeltmeleri.
- `41722aa8fbc0b42faa99be786cda57f25be12a42` yerel rapor/yedek dışlamalarını genişletti.
- Sonraki beş commit tarihli devir indeksi, yeni kurulum/çalışma rehberi ve klon sonrası yerel veri ayrımını ekledi.

## Doğrulama ve çalıştırılmayanlar

Çalıştırılan salt okunur kontroller:

- `git status --short --branch`, `git branch --show-current`, `git rev-parse HEAD`, `git remote -v`
- `git worktree list --porcelain` ve iki ilgili checkout'ta ayrı `status`/HEAD kontrolü
- `git fetch --prune origin`
- `git rev-list --left-right --count`, `git merge-base`, `git log`, `git diff --name-status` ve `git diff --stat`
- Yeni modüllerdeki sınıf/fonksiyonlar ile GUI bağlantı noktalarının `rg` üzerinden incelenmesi

Unittest, derleme, GUI smoke ve gerçek API/Batch çalıştırılmadı; uygulama kodu değişmedi ve görev Git farklarını/devralınan özellikleri saptamaktı. Dosya silme, checkout değiştirme, pull, merge, rebase veya bağımlılık kurulumu yapılmadı.

## Bağımlılıklar, ayarlar ve açık işler

Yeni bağımlılık, ayar veya veri şeması değişikliği yoktur. Önceki teknik notta açık bırakılan son düzeltmeler sonrası tam test paketi ve gerçek API/Batch uçtan uca doğrulaması bu salt okunur devir incelemesinin dışında kaldı.

Bu not commit edilmeden önce uzak dal eşitti. Not/indeks commit'inden sonra normal push yapılacak; GitHub'a yükleme yalnız uzak `master` hash'i yerel HEAD ile eşleşirse doğrulanmış sayılacaktır.
