# Devir Notu — 2026-09-15 10:09

## Oturum özeti

Kullanıcı isteği (iki aşamalı):

1. Önce "browser tarafına odaklan, kod değiştirmeden bug ve geliştirme
   önerileri raporla" — önceki oturumun devamı.
2. Ardından düzeltme: "tarayıcı tarafı diye bir şey yoktu, genel bugları
   bul ve geliştirme önerileri ver; bittiğinde mutlaka bir rapor dosyası
   oluştur, önceki bulgular da dahil."

Bu oturum genel salt-okuma denetim yapıldı ve konsolide rapor yazıldı:
`GENEL_BUG_VE_GELISTIRME_RAPORU_2026-09-15.md` (köke eklendi).

## Repo durumu

- Repo: `openai-altyazi-cevirisi` (`github.com/dorukakindev/openai-altyazi-cevirisi`)
- Dal: `master`
- Başlangıç HEAD: `5a7cda9`
- Bitiş kod HEAD: `5a7cda9` (kod değişikliği yok)
- Çalışma ağacı: yalnız önceki oturumlardan kalan untracked rapor/plan
  dosyaları; kullanıcı dosyalarına dokunulmadı.

## Değişen dosyalar

- `GENEL_BUG_VE_GELISTIRME_RAPORU_2026-09-15.md` — YENİ. Konsolide bug ve
  geliştirme raporu: 11 yeni bulgu (B1–B11), önceki raporlardan güncel
  kodda hâlâ açık 12 madde (A1–A12), düzeltilmiş/geçersiz eski iddialar,
  öncelikli geliştirme önerileri, regresyon testi önerileri.
- `docs/devir/2026-09-15-1009.md` — bu not.
- `DEVIR-NOTU.md` — indeks güncellendi.

## İncelenen alanlar

- `okuma_yuzeyi.py` (tamamı): eşleştirme algoritması, HTML/JS çıktısı.
- `chunk_gunlugu.py` + `chunk_sorgu.py` (tamamı): kayıt/sorgu/replay/budama.
- `translation_memory.py`: fuzzy/exact kapsam asimetrisi, `_init_db` ALTER
  yarışı, LIMIT 500, ölü `_fuzzy_semantic_anchors`.
- `series_memory.py`: `parse_series_key`, `_tv_root_info`, `season_pos==0`.
- `subtitle_batch_translate.py`, `repair_batches.py`, `resume_batch.py`,
  `read_errors.py`, `app_state.mutate_batch_ids`.
- `subtitle_formats.py`: `_vtt_ts_to_srt`, `_ass_ts_to_srt`,
  `_strip_srt_unsafe_ass_overrides`.
- `sdh_cleaner.py:812-868`: `is_sdh_descriptor` sound-noun dalı (A1 hâlâ açık).
- `helper_models.py:715-730`: `is_anthropic_native` `/v1` kontrolü (A7 hâlâ açık).
- `subtitle_translator_gui.py` hedefli bölgeler: `_load_settings` (BUG 5
  düzeltilmiş görüldü), `_ts_field_seconds`, `_rebalanced_two_lines`,
  `_chunk_gunluk_*`, okuma yüzeyi üretim/diyalog entegrasyonu.
- Önceki raporların madde bazında durum doğrulaması (2026-09-02 Tur, 09-04
  Tur1+Tur2, 09-11 kök raporu, BROWSER_* serisi).

## Önemli bulgular (özet)

Yeni: B1 Ctrl+P/N kısayol engeli · B2 numara-fallback ts eşleşmesini çalıyor ·
B3 `gecmis --dosya` chunk'lara uygulanmıyor · B4 replay model/rol sadakati ·
B5 `budan(sinir=0)` her şeyi siler · B6 TM fuzzy/exact kapsam asimetrisi ·
B7 `.okuma.html` kullanıcıya gösterilmiyor · B8 üç farklı sağlamlıkta zaman
ayrıştırıcı · B9 yinelenen `id="cN"` · B10 replay'de retry/safe-create yok ·
B11 standalone `parse_srt` index varsayımı.

Hâlâ açık eski: A1 SDH yanlış-pozitif · A2 read_errors logs/ · A3 ölü
`srt_files` · A4 ölü `_fuzzy_semantic_anchors` · A5 Türkçe küfür kuralı ·
A6 `season_pos==0` · A7 `/v1` auth · A8 tag'ı bölen rebalance · A9 `{\an8}`
silinmesi · A10 artakalan sayfa sonu · A11 BOM'lu batch_id · A12 `_TS_RE`.

Düzeltilmiş: `_load_settings` ValueError; `_ts_field_seconds` metadata.
Geçersiz: BROWSER raporundaki arama/video/localStorage/drag-drop maddeleri
(mevcut `okuma_yuzeyi.py`'de bu özellikler yok).

## Testler

- Bu oturumda test süiti çalıştırılmadı (salt-okuma denetim, kod değişikliği
  yok). Önceki turda 5431 test OK idi.
- Raporda her bulgu için önerilen regresyon testleri BÖLÜM 5'te listeli.

## Açık işler / sonraki adımlar

- Kullanıcı onayıyla B1/B2/B3/B5 gibi düşük-maliyetli düzeltmelerle
  başlanabilir; her biri için rapordaki test önerisi birlikte uygulanmalı.
- Tur 1 (0902) ve YENI_MODULLER serisinin kalan maddeleri için ayrı durum
  doğrulama turu gerekli.
- `parse_srt` birleştirme ve okuma yüzeyi "Tarayıcıda Aç" düğmesi mimari
  karar gerektiriyor.

## Notlar

- API anahtarı/kişisel veri rapora taşınmadı; makineye özgü yollar
  göreli/depo içi biçimde yazıldı.
