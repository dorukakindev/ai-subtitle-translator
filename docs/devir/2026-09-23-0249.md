# Main checkout sync — 2026-09-23 02:49 (Europe/Istanbul)

## Repository and commits

- Repository: `dorukakindev/ai-subtitle-translator`; branch: `master`.
- Starting code commit: `86b4acb63a554075ed3eb69c468bb78a3258efbd`.
- Ending code commit: `cf381223ba575613cb75e04daed5619d93282519`.
- After explicit user approval, the main checkout fast-forwarded 15 existing upstream commits. This includes the English-first interface, merged Devin PRs #1–#8, and their earlier handoff notes. No application code was authored in this session.

## Data preservation and scope

- The upstream public-repository cleanup removes 89 previously tracked archive, subtitle, plan, and internal-document files from the working tree. Before the fast-forward, all 89 were copied into the ignored local `Raporlar/local-pre-public-sync-2026-09-23/` backup; each copy matched its source SHA-256 hash.
- The 16 pre-existing untracked audit/report files remained in place and were not staged or committed. Existing ignored settings, memories, and translation outputs were not moved.
- The new public checkout and this main checkout now point to the same code commit. The local backup is private and was not added to Git.

## Validation

- `git merge --ff-only origin/master` succeeded; local `master` matched `origin/master` before this note.
- `.venv/Scripts/python.exe -m py_compile subtitle_translator_gui.py hybrid_translate.py subtitle_formats.py ui_localization.py` passed.
- `.venv/Scripts/python.exe -m unittest tests.test_delivery_failure_runtime tests.test_auto_glossary_parity tests.test_series_memory_report_line tests.test_ui_localization` passed: 45 tests, OK. Test-harness temporary-log cleanup warnings did not fail the suite.
- An isolated `App()` construction, idle update, and destruction passed: `GUI_OK`.
- The full test suite and live paid API translation were not run in this session. The full suite passed during the preceding PR review, before this local fast-forward.

## Dependencies, settings, and remaining work

- No dependency was installed and no manual database or settings migration was performed. Upstream's English-first UI setting and application changes came with the fast-forward.
- The merged GUI testing skill still contains machine-specific Ubuntu instructions and suggests deleting a settings file to reset first-run state; its author should generalize that guidance. It was not used for this Windows verification.
- Only this dated note and the handoff index are authored changes; their separate commit and remote verification follow.
